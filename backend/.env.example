# Connection string de Neon (Postgres). Ejemplo de formato:
# postgresql://user:password@host/dbname?sslmode=require
DATABASE_URL=

# Secreto para firmar los JWT. Usar un valor largo y aleatorio.
JWT_SECRET=

# Puerto en el que corre el servidor Express
PORT=3000
