// Placeholder — se construye entero en el paso siguiente al de Cuentas
// (listado con filtros + modal de crear/editar transacción).
export function Transactions() {
  return (
    <div className="flex min-h-[60vh] items-center justify-center">
      <p className="font-sans text-body-md text-on-surface-variant">Transacciones: próximo paso.</p>
    </div>
  );
}
